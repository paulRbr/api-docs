{
  "type": "object",
  "required": ["results"],
  "properties": {
    "results": {
      "type": "array",
      "description": "An array of information for all products within the requested range.",
      "maxItems": 1000,
      "items": {
        "$ref": "./Product.json"
      }
    },
    "paging": { "$ref": "./Pagination.json" }
  }
}
