{
  "type": "object",
  "required": [
    "min_pay_percentage"
  ],
  "properties": {
    "min_pay_percentage": {
      "type": "number",
      "description": "The percentage of total balance calculated for min pay. (i.e. 20 means 20%)",
      "example": 20
    },
    "roll_to_amortization": {
      "type": "boolean",
      "description": "Set false for revolving products. If applicable, a boolean to indicate whether an account's balance rolls to amortization. Note - if there are any promotions configured for the product, it will roll to amortization *at the end of the promotion*. Otherwise, as in a standard installment product, it will roll to amortization upon origination.",
      "example": true
    },
    "delinquent_on_n_consecutive_late_fees": {
      "type": "integer",
      "description": "After this number of consecutive late events, the account status shifts to `suspended` with a status subtype of `delinquent`"
    },
    "suspend_on_n_consecutive_late_fees": {
      "type": "integer",
      "description": "After this number of consecutive late events, the account status shifts to `suspended` with a status subtype of `charged_off`"
    }

  },
  "description": "The set of policies governing payment due information for the product."
}