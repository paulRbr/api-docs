{
  "type": "object",
  "required": ["payment_processor_name", "merchant_config"],
  "properties": {
    "payment_processor_name": {
      "type": "string",
      "enum": ["NONE", "REPAY", "DWOLLA", "MODERN_TREASURY"],
      "description": "Indicates the payment processor for which the merchant config is being set.",
      "example": "REPAY",
      "default": "NONE"
    },
    "merchant_config": {
      "type": "string",
      "description": "The merchant config value obtained from the corresponding payment processor.",
      "example": "87397472"
    }
  }
}
