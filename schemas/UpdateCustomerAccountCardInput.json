{
  "type": "object",
  "properties": {
    "spend_limit": {
      "type": "integer",
      "nullable": true,
      "minimum": 1,
      "description": "Spending limit of card (in cents). If not provided, spending is still subject to the spend limit set at the issuer processor account (if any).",
      "example": 8000000
    },
    "lithic": {
      "type": "object",
      "required": ["token"],
      "properties": {
        "token": {
          "type": "string",
          "description": "External identifier for card in Lithic.",
          "example": "5c7678a6-db0e-4c24-9ad4-73eb0f719724"
        },
        "memo": {
          "type": "string",
          "description": "Friendly name to identify the card",
          "example": "Primary Business Card"
        },
        "state": {
          "type": "string",
          "enum": ["OPEN", "PAUSED", "CLOSED"],
          "description": "The new state of the card. **Note: \"CLOSED\" cannot be undone.**",
          "example": "OPEN"
        }
      }
    }
  }
}
