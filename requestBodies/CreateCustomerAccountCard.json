{
  "description": "Create a card directly with the configured issuer processor for a customer on an account",
  "content": {
    "application/json": {
      "schema": {
        "$ref": "../schemas/CreateCustomerAccountCardInput.json"
      }
    }
  }
}
