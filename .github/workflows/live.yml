name: Deploy documentation to live docs site

on:
  push:
    branches:
      - production

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Check out latest code
      - uses: actions/checkout@v2

      - uses: actions/cache@v2
        id: cache
        with:
          path: "**/node_modules"
          key: ${{ runner.os }}-modules-${{ hashFiles('**/yarn.lock') }}

      - name: Install node_modules
        if: steps.cache.outputs.cache-hit != 'true'
        run: yarn

      - name: Bundle into one file
        run: yarn bundle

      - name: Validate
        run: yarn validate

      - name: Deploy to internal documentation
        uses: bump-sh/github-action@0.1
        with:
          id: ${{ secrets.BUMP_DOC_ID_LIVE }}
          token: ${{ secrets.BUMP_TOKEN_LIVE }}
          file: dist/consolidated.json
